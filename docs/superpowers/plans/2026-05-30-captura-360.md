# Captura 360° desde el Sistema — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Permitir al personal capturar fotos con el celular desde el navegador, unirlas automáticamente en una imagen equirectangular y subirla al tour 360° sin apps externas.

**Architecture:** El frontend abre la cámara trasera y el giroscopio para guiar la captura de ~12–20 fotos en diferentes ángulos; al finalizar las envía al backend Node.js que llama un script Python con OpenCV para coserlas en un panorama 2:1 y guardarlo en `uploads/habitaciones/360/`.

**Tech Stack:** React + MediaDevices API + DeviceOrientation API + Canvas API | Node.js + multer + child_process | Python 3 + opencv-python + Pillow + numpy

---

## Mapa de archivos

| Archivo | Acción | Responsabilidad |
|---|---|---|
| `backend/scripts/stitch360.py` | Crear | Recibe carpeta de fotos → cose con OpenCV → guarda equirectangular |
| `backend/config/multer.js` | Modificar | Agregar `uploadTemp` para recibir múltiples fotos en carpeta UUID |
| `backend/controllers/habitacion.controller.js` | Modificar | Agregar `capturarFotos360` — llama Python, guarda en BD |
| `backend/routes/habitacion.routes.js` | Modificar | Agregar `POST /:id/capturar-360` |
| `frontend/src/services/habitacion.js` | Modificar | Agregar `capturaFotos360(idHabitacion, fotos, titulo)` |
| `frontend/src/pages/habitacion/CapturaAsistente360.jsx` | Crear | Modal fullscreen: cámara + guía giroscopio + envío |
| `frontend/src/pages/habitacion/Imagenes360Manager.jsx` | Modificar | Botón "Capturar con cámara" que abre el asistente |

---

## Task 1: Instalar dependencias Python

**Files:**
- (ningún archivo del proyecto — solo instalación en el entorno)

- [ ] **Step 1: Instalar opencv-python, numpy y Pillow**

```bash
pip install opencv-python numpy Pillow
```

- [ ] **Step 2: Verificar instalación**

```bash
python -c "import cv2, numpy, PIL; print('OK', cv2.__version__)"
```

Salida esperada: `OK 4.x.x` (o la versión instalada)

---

## Task 2: Script Python de stitching

**Files:**
- Crear: `backend/scripts/stitch360.py`

- [ ] **Step 1: Crear carpeta scripts si no existe**

```bash
mkdir -p backend/scripts
```

- [ ] **Step 2: Crear el script**

```python
# backend/scripts/stitch360.py
import sys
import os
import cv2
import numpy as np
from PIL import Image

def stitch(input_folder, output_path):
    exts = ('.jpg', '.jpeg', '.png')
    files = sorted([
        os.path.join(input_folder, f)
        for f in os.listdir(input_folder)
        if f.lower().endswith(exts)
    ])

    if len(files) < 4:
        print("ERROR: Se necesitan al menos 4 imágenes", file=sys.stderr)
        sys.exit(1)

    images = [cv2.imread(f) for f in files]
    images = [img for img in images if img is not None]

    if len(images) < 4:
        print("ERROR: No se pudieron leer las imágenes", file=sys.stderr)
        sys.exit(1)

    stitcher = cv2.Stitcher_create(cv2.Stitcher_PANORAMA)
    status, panorama = stitcher.stitch(images)

    if status != cv2.Stitcher_OK:
        codes = {
            cv2.Stitcher_ERR_NEED_MORE_IMGS: "pocas imágenes o poca superposición",
            cv2.Stitcher_ERR_HOMOGRAPHY_EST_FAIL: "no se encontraron puntos comunes",
            cv2.Stitcher_ERR_CAMERA_PARAMS_ADJUST_FAIL: "error al ajustar parámetros",
        }
        msg = codes.get(status, f"código {status}")
        print(f"ERROR: No se pudo coser el panorama — {msg}", file=sys.stderr)
        sys.exit(2)

    # Ajustar a proporción 2:1 (equirectangular)
    rgb = cv2.cvtColor(panorama, cv2.COLOR_BGR2RGB)
    pil = Image.fromarray(rgb)
    w, h = pil.size
    target_w = max(w, 4096)
    target_h = target_w // 2
    pil = pil.resize((target_w, target_h), Image.LANCZOS)

    os.makedirs(os.path.dirname(output_path), exist_ok=True)
    pil.save(output_path, 'JPEG', quality=90)
    print(output_path)

if __name__ == '__main__':
    if len(sys.argv) != 3:
        print("Uso: python stitch360.py <carpeta_entrada> <ruta_salida>", file=sys.stderr)
        sys.exit(1)
    stitch(sys.argv[1], sys.argv[2])
```

- [ ] **Step 3: Probar el script con imágenes reales**

Crear carpeta de prueba y copiar 4+ fotos solapadas:

```bash
mkdir -p backend/scripts/test_fotos
# copiar fotos manualmente a esa carpeta, luego:
python backend/scripts/stitch360.py backend/scripts/test_fotos uploads/habitaciones/360/test-output.jpg
```

Salida esperada: ruta del archivo de salida impresa en stdout.

- [ ] **Step 4: Commit**

```bash
git add backend/scripts/stitch360.py
git commit -m "feat: agregar script Python de stitching de fotos 360"
```

---

## Task 3: Multer para fotos temporales

**Files:**
- Modificar: `backend/config/multer.js`

- [ ] **Step 1: Agregar storage temporal al final de multer.js**

Abrir `backend/config/multer.js` y agregar al final, antes del último comentario de exportación:

```javascript
// ============================
// CONFIGURACIÓN PARA FOTOS TEMPORALES (captura 360°)
// ============================
const storageTemp = multer.diskStorage({
  destination: (req, file, cb) => {
    // Cada request usa su propia carpeta UUID para aislar sesiones paralelas
    if (!req.tempFolderPath) {
      req.tempFolderPath = `uploads/temp/${Date.now()}-${Math.round(Math.random() * 1e9)}`;
    }
    if (!fs.existsSync(req.tempFolderPath)) {
      fs.mkdirSync(req.tempFolderPath, { recursive: true });
    }
    cb(null, req.tempFolderPath);
  },
  filename: (req, file, cb) => {
    const idx = Date.now() + '-' + Math.round(Math.random() * 1e9);
    cb(null, `foto-${idx}.jpg`);
  }
});

// Hasta 30 fotos, 8 MB cada una
export const uploadTemp = multer({
  storage: storageTemp,
  fileFilter: fileFilter,
  limits: { fileSize: 8 * 1024 * 1024, files: 30 }
});
```

- [ ] **Step 2: Verificar que el archivo compila sin errores**

```bash
cd backend && node --input-type=module < /dev/null 2>&1 || node -e "import('./config/multer.js').then(()=>console.log('OK'))"
```

En Windows PowerShell:

```powershell
cd backend; node -e "import('./config/multer.js').then(()=>console.log('OK')).catch(e=>console.error(e.message))"
```

Salida esperada: `OK`

- [ ] **Step 3: Commit**

```bash
git add backend/config/multer.js
git commit -m "feat: agregar uploadTemp en multer para captura 360"
```

---

## Task 4: Controlador capturarFotos360

**Files:**
- Modificar: `backend/controllers/habitacion.controller.js`

- [ ] **Step 1: Agregar imports al inicio del controlador**

Al inicio de `backend/controllers/habitacion.controller.js`, después de los imports existentes, agregar:

```javascript
import { execFile } from 'child_process';
import { promisify } from 'util';
import { v4 as uuidv4 } from 'uuid';

const execFileAsync = promisify(execFile);
```

> Nota: `uuid` ya está disponible en Node. Si falla, instalar con `npm install uuid` en backend.

- [ ] **Step 2: Agregar la función al final del archivo**

Al final de `backend/controllers/habitacion.controller.js` agregar:

```javascript
// ============================
// CAPTURAR FOTOS Y CREAR IMAGEN 360°
// ============================
export const capturarFotos360 = async (req, res) => {
  const { id } = req.params;
  const { titulo, descripcion } = req.body;
  const fotos = req.files;

  if (!fotos || fotos.length < 4) {
    return res.status(400).json({ message: 'Se necesitan al menos 4 fotos para crear la imagen 360°' });
  }

  const tempFolder = req.tempFolderPath;
  const outputFilename = `360-${Date.now()}-${Math.round(Math.random() * 1e9)}.jpg`;
  const outputPath = path.join('uploads/habitaciones/360', outputFilename);

  if (!fs.existsSync('uploads/habitaciones/360')) {
    fs.mkdirSync('uploads/habitaciones/360', { recursive: true });
  }

  try {
    // Ruta absoluta al script
    const scriptPath = path.join(process.cwd(), 'scripts', 'stitch360.py');

    await execFileAsync('python', [scriptPath, tempFolder, outputPath], {
      timeout: 90000 // 90 segundos máximo
    });

    // Orden siguiente en la BD
    const [rows] = await db.query(
      `SELECT COALESCE(MAX(orden), 0) + 1 AS next_orden
       FROM habitacion_imagen
       WHERE id_habitacion = ? AND tipo_imagen = '360'`,
      [id]
    );
    const orden = rows[0].next_orden;

    await db.query(
      `INSERT INTO habitacion_imagen (id_habitacion, ruta, tipo_imagen, titulo, descripcion, orden)
       VALUES (?, ?, '360', ?, ?, ?)`,
      [id, outputFilename, titulo || 'Vista 360°', descripcion || '', orden]
    );

    // Limpiar fotos temporales
    fs.rmSync(tempFolder, { recursive: true, force: true });

    res.json({ message: 'Imagen 360° creada exitosamente', ruta: outputFilename });

  } catch (error) {
    // Limpiar siempre en caso de error
    if (tempFolder && fs.existsSync(tempFolder)) {
      fs.rmSync(tempFolder, { recursive: true, force: true });
    }
    console.error('❌ Error en capturarFotos360:', error.stderr || error.message);
    res.status(500).json({
      message: 'No se pudo crear la imagen 360°',
      detalle: error.stderr || error.message
    });
  }
};
```

- [ ] **Step 3: Commit**

```bash
git add backend/controllers/habitacion.controller.js
git commit -m "feat: agregar controlador capturarFotos360"
```

---

## Task 5: Ruta POST capturar-360

**Files:**
- Modificar: `backend/routes/habitacion.routes.js`

- [ ] **Step 1: Agregar import del nuevo controlador y de uploadTemp**

En `backend/routes/habitacion.routes.js`, en el bloque de imports del controlador agregar `capturarFotos360`:

```javascript
import {
  listarHabitaciones,
  obtenerHabitacion,
  crearHabitacion,
  editarHabitacion,
  eliminarHabitacion,
  cambiarEstadoHabitacion,
  subirImagenes360,
  listarImagenes360,
  eliminarImagen360,
  actualizarImagen360,
  capturarFotos360,           // ← agregar esta línea
  getHabitacionesPublicas,
  getHabitacionDetalle,
  verificarDisponibilidad,
  getTiposHabitacion,
  obtenerFechasOcupadas
} from '../controllers/habitacion.controller.js';
import { upload, upload360, uploadTemp } from '../config/multer.js';  // ← agregar uploadTemp
```

- [ ] **Step 2: Agregar la ruta al bloque de rutas 360**

Al final del bloque de rutas 360°, antes de `export default router;`:

```javascript
// Capturar fotos con cámara y crear imagen 360° automáticamente
router.post('/:id/capturar-360',
  authMiddleware,
  initAbility,
  checkAbility('update', 'Habitacion'),
  uploadTemp.array('fotos[]', 30),
  capturarFotos360
);
```

- [ ] **Step 3: Commit**

```bash
git add backend/routes/habitacion.routes.js
git commit -m "feat: agregar ruta POST capturar-360"
```

---

## Task 6: Servicio frontend capturaFotos360

**Files:**
- Modificar: `frontend/src/services/habitacion.js`

- [ ] **Step 1: Agregar la función al final del archivo**

En `frontend/src/services/habitacion.js` al final del bloque de IMÁGENES 360°:

```javascript
export const capturaFotos360 = async (idHabitacion, fotosBlob, titulo = 'Vista 360°', descripcion = '') => {
  const formData = new FormData();
  fotosBlob.forEach((blob, i) => {
    formData.append('fotos[]', blob, `foto-${i}.jpg`);
  });
  formData.append('titulo', titulo);
  formData.append('descripcion', descripcion);

  const { data } = await api.post(
    `/habitaciones/${idHabitacion}/capturar-360`,
    formData,
    {
      headers: { 'Content-Type': 'multipart/form-data' },
      timeout: 120000 // 2 minutos — el stitching puede tardar
    }
  );
  return data;
};
```

- [ ] **Step 2: Commit**

```bash
git add frontend/src/services/habitacion.js
git commit -m "feat: agregar servicio capturaFotos360 en frontend"
```

---

## Task 7: Componente CapturaAsistente360

**Files:**
- Crear: `frontend/src/pages/habitacion/CapturaAsistente360.jsx`

- [ ] **Step 1: Crear el componente completo**

```jsx
// frontend/src/pages/habitacion/CapturaAsistente360.jsx
import { useState, useEffect, useRef, useCallback } from 'react';
import { capturaFotos360 } from '../../services/habitacion';

// 8 direcciones horizontales × 2 verticales (arriba/medio) = 16 zonas útiles
const SECTORES_H = ['N', 'NE', 'E', 'SE', 'S', 'SO', 'O', 'NO'];
const MIN_FOTOS = 6;

const getZona = (alpha, beta) => {
  const h = Math.floor(((alpha ?? 0) + 22.5) % 360 / 45); // 0-7
  const v = (beta ?? 0) < -20 ? 'U' : 'M'; // arriba o medio
  return `${h}-${v}`;
};

const CapturaAsistente360 = ({ habitacion, onClose, onSuccess }) => {
  const videoRef    = useRef(null);
  const canvasRef   = useRef(null);
  const streamRef   = useRef(null);
  const orientRef   = useRef({ alpha: 0, beta: 0 });
  const lastZoneRef = useRef(null);
  const captureTimerRef = useRef(null);

  const [fotos,        setFotos]        = useState([]);   // array de Blob
  const [zonasCubiertas, setZonasCubiertas] = useState(new Set());
  const [zonaActual,   setZonaActual]   = useState('');
  const [flash,        setFlash]        = useState(false);
  const [procesando,   setProcesando]   = useState(false);
  const [error,        setError]        = useState('');
  const [titulo,       setTitulo]       = useState('Vista 360°');
  const [camActiva,    setCamActiva]    = useState(false);
  const [sinGiroscopio, setSinGiroscopio] = useState(false);

  // ── Abrir cámara ──────────────────────────────────────
  useEffect(() => {
    let cancelled = false;
    (async () => {
      try {
        const stream = await navigator.mediaDevices.getUserMedia({
          video: { facingMode: 'environment', width: { ideal: 1920 }, height: { ideal: 1080 } },
          audio: false
        });
        if (cancelled) { stream.getTracks().forEach(t => t.stop()); return; }
        streamRef.current = stream;
        if (videoRef.current) {
          videoRef.current.srcObject = stream;
          videoRef.current.play();
          setCamActiva(true);
        }
      } catch (e) {
        setError('No se pudo acceder a la cámara. Verifica los permisos del navegador.');
      }
    })();
    return () => {
      cancelled = true;
      streamRef.current?.getTracks().forEach(t => t.stop());
    };
  }, []);

  // ── Giroscopio ───────────────────────────────────────
  useEffect(() => {
    let gotOrientation = false;
    const onOrientation = (e) => {
      if (!gotOrientation) { gotOrientation = true; setSinGiroscopio(false); }
      orientRef.current = { alpha: e.alpha ?? 0, beta: e.beta ?? 0 };
      const zona = getZona(e.alpha, e.beta);
      setZonaActual(zona);
    };

    // Solicitar permiso en iOS 13+
    if (typeof DeviceOrientationEvent?.requestPermission === 'function') {
      DeviceOrientationEvent.requestPermission()
        .then(p => { if (p === 'granted') window.addEventListener('deviceorientation', onOrientation); })
        .catch(() => setSinGiroscopio(true));
    } else {
      window.addEventListener('deviceorientation', onOrientation);
      setTimeout(() => { if (!gotOrientation) setSinGiroscopio(true); }, 2000);
    }

    return () => window.removeEventListener('deviceorientation', onOrientation);
  }, []);

  // ── Auto-captura cuando zona cambia ──────────────────
  useEffect(() => {
    if (!camActiva || !zonaActual || procesando) return;

    if (zonaActual === lastZoneRef.current) return;
    if (zonasCubiertas.has(zonaActual)) return;

    // Esperar 600ms estable en la zona antes de capturar
    clearTimeout(captureTimerRef.current);
    captureTimerRef.current = setTimeout(() => {
      if (zonaActual !== lastZoneRef.current) return; // se movió
      capturarFoto();
    }, 600);

    lastZoneRef.current = zonaActual;
    return () => clearTimeout(captureTimerRef.current);
  }, [zonaActual, camActiva, procesando]);

  // ── Capturar fotograma ───────────────────────────────
  const capturarFoto = useCallback(() => {
    const video  = videoRef.current;
    const canvas = canvasRef.current;
    if (!video || !canvas || !camActiva) return;

    canvas.width  = video.videoWidth  || 1280;
    canvas.height = video.videoHeight || 720;
    canvas.getContext('2d').drawImage(video, 0, 0);

    canvas.toBlob((blob) => {
      if (!blob) return;
      setFotos(prev => [...prev, blob]);
      setZonasCubiertas(prev => new Set([...prev, zonaActual]));
      setFlash(true);
      setTimeout(() => setFlash(false), 200);
    }, 'image/jpeg', 0.92);
  }, [camActiva, zonaActual]);

  // ── Enviar al backend ────────────────────────────────
  const handleEnviar = async () => {
    if (fotos.length < MIN_FOTOS) {
      setError(`Necesitas al menos ${MIN_FOTOS} fotos. Tienes ${fotos.length}.`);
      return;
    }
    setProcesando(true);
    setError('');
    try {
      await capturaFotos360(habitacion.id_habitacion, fotos, titulo);
      onSuccess();
      onClose();
    } catch (e) {
      setError(e?.response?.data?.detalle || e?.response?.data?.message || 'Error al procesar las fotos');
    } finally {
      setProcesando(false);
    }
  };

  const progreso = Math.min(100, Math.round((fotos.length / 16) * 100));

  return (
    <div className="fixed inset-0 z-[300] flex flex-col bg-black">
      {/* Flash de captura */}
      {flash && <div className="absolute inset-0 z-50 bg-white opacity-60 pointer-events-none" />}

      {/* Video */}
      <video
        ref={videoRef}
        className="absolute inset-0 w-full h-full object-cover"
        playsInline muted
      />
      <canvas ref={canvasRef} className="hidden" />

      {/* Overlay oscuro top */}
      <div className="relative z-10 flex items-center justify-between px-4 py-3 bg-black/60 backdrop-blur-sm">
        <button
          onClick={onClose}
          className="flex items-center gap-2 text-white text-sm font-medium"
        >
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth={2} className="w-5 h-5">
            <path strokeLinecap="round" strokeLinejoin="round" d="M19 12H5M12 5l-7 7 7 7"/>
          </svg>
          Cancelar
        </button>
        <p className="text-white text-sm font-semibold">
          Captura 360° — Hab. {habitacion.numero}
        </p>
        <span className="text-white/60 text-sm">{fotos.length} fotos</span>
      </div>

      {/* Barra de progreso */}
      <div className="relative z-10 h-1.5 bg-white/20">
        <div
          className="h-full bg-green-400 transition-all duration-300"
          style={{ width: `${progreso}%` }}
        />
      </div>

      {/* Compass de zonas cubiertas */}
      <div className="relative z-10 flex justify-center py-4">
        <div className="relative w-32 h-32">
          {/* Anillo exterior */}
          <div className="absolute inset-0 rounded-full border-2 border-white/30" />
          {/* Punto central */}
          <div className="absolute inset-0 flex items-center justify-center">
            <div className="w-2 h-2 rounded-full bg-white/60" />
          </div>
          {/* Sectores */}
          {SECTORES_H.map((dir, i) => {
            const angle = i * 45 - 90; // en grados
            const rad   = (angle * Math.PI) / 180;
            const r     = 48;
            const x     = 64 + r * Math.cos(rad);
            const y     = 64 + r * Math.sin(rad);
            const cubiertoM = zonasCubiertas.has(`${i}-M`);
            const cubiertoU = zonasCubiertas.has(`${i}-U`);
            return (
              <div
                key={dir}
                className="absolute flex flex-col items-center gap-0.5"
                style={{ left: x - 10, top: y - 10, width: 20 }}
              >
                {/* punto vertical arriba */}
                <div className={`w-2 h-2 rounded-full border ${cubiertoU ? 'bg-green-400 border-green-400' : 'bg-transparent border-white/30'}`} />
                {/* punto horizontal medio */}
                <div className={`w-3 h-3 rounded-full border-2 ${cubiertoM ? 'bg-green-400 border-green-400' : 'bg-transparent border-white/50'}`} />
                <span className="text-white/50 text-[8px] leading-none">{dir}</span>
              </div>
            );
          })}
        </div>
      </div>

      {/* Instrucción */}
      <div className="relative z-10 flex-1 flex flex-col items-center justify-center gap-2 pointer-events-none">
        {sinGiroscopio ? (
          <div className="bg-amber-500/80 rounded-xl px-4 py-2 text-center">
            <p className="text-white text-sm font-medium">Sin giroscopio detectado</p>
            <p className="text-white/80 text-xs mt-0.5">Usa el botón para capturar manualmente</p>
          </div>
        ) : (
          <div className="bg-black/50 rounded-full px-5 py-2">
            <p className="text-white text-sm">
              Apunta en todas direcciones — se captura solo
            </p>
          </div>
        )}
      </div>

      {/* Controles bottom */}
      <div className="relative z-10 bg-black/70 backdrop-blur-sm px-4 pb-safe pt-3 space-y-3">
        {error && (
          <div className="bg-red-500/20 border border-red-400/50 rounded-xl px-3 py-2 text-red-200 text-sm text-center">
            {error}
          </div>
        )}

        {/* Título */}
        <input
          type="text"
          value={titulo}
          onChange={e => setTitulo(e.target.value)}
          placeholder="Nombre de esta vista (ej: Sala principal)"
          className="w-full bg-white/10 border border-white/20 rounded-xl px-4 py-2.5 text-white text-sm placeholder-white/40 focus:outline-none focus:border-white/50"
        />

        <div className="flex gap-3">
          {/* Captura manual */}
          <button
            onClick={capturarFoto}
            disabled={!camActiva || procesando}
            className="flex-1 flex items-center justify-center gap-2 bg-white/15 hover:bg-white/25 disabled:opacity-40 border border-white/20 text-white rounded-xl py-3 text-sm font-medium transition-colors"
          >
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth={2} className="w-5 h-5">
              <path strokeLinecap="round" strokeLinejoin="round" d="M3 9a2 2 0 012-2h.93a2 2 0 001.664-.89l.812-1.22A2 2 0 0110.07 4h3.86a2 2 0 011.664.89l.812 1.22A2 2 0 0018.07 7H19a2 2 0 012 2v9a2 2 0 01-2 2H5a2 2 0 01-2-2V9z"/>
              <circle cx="12" cy="13" r="3"/>
            </svg>
            Capturar ({fotos.length})
          </button>

          {/* Procesar */}
          <button
            onClick={handleEnviar}
            disabled={fotos.length < MIN_FOTOS || procesando}
            className="flex-1 flex items-center justify-center gap-2 bg-green-500 hover:bg-green-600 disabled:bg-green-900/50 disabled:text-white/40 text-white rounded-xl py-3 text-sm font-bold transition-colors"
          >
            {procesando ? (
              <>
                <svg className="w-4 h-4 animate-spin" viewBox="0 0 24 24" fill="none">
                  <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4"/>
                  <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z"/>
                </svg>
                Procesando…
              </>
            ) : (
              <>
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth={2} className="w-5 h-5">
                  <path strokeLinecap="round" strokeLinejoin="round" d="M4 16v1a3 3 0 003 3h10a3 3 0 003-3v-1m-4-8l-4-4m0 0L8 8m4-4v12"/>
                </svg>
                {fotos.length < MIN_FOTOS ? `Falta ${MIN_FOTOS - fotos.length}` : 'Crear 360°'}
              </>
            )}
          </button>
        </div>

        <p className="text-center text-white/30 text-xs pb-1">
          Mínimo {MIN_FOTOS} fotos · Recomendado 12–20 con superposición
        </p>
      </div>
    </div>
  );
};

export default CapturaAsistente360;
```

- [ ] **Step 2: Commit**

```bash
git add frontend/src/pages/habitacion/CapturaAsistente360.jsx
git commit -m "feat: componente CapturaAsistente360 con cámara y giroscopio"
```

---

## Task 8: Integrar botón en Imagenes360Manager

**Files:**
- Modificar: `frontend/src/pages/habitacion/Imagenes360Manager.jsx`

- [ ] **Step 1: Agregar import del asistente**

Al inicio de `frontend/src/pages/habitacion/Imagenes360Manager.jsx`, después de los imports existentes:

```javascript
import CapturaAsistente360 from './CapturaAsistente360';
```

- [ ] **Step 2: Agregar estado para el asistente**

Dentro del componente, junto a los estados existentes:

```javascript
const [mostrarCaptura, setMostrarCaptura] = useState(false);
```

- [ ] **Step 3: Agregar botón "Capturar con cámara" en el header del modal**

En el header del componente (junto al botón de cerrar ×), agregar antes de ese botón:

```jsx
<button
  onClick={() => setMostrarCaptura(true)}
  className="flex items-center gap-1.5 bg-white/20 hover:bg-white/30 text-white text-sm font-medium px-3 py-1.5 rounded-lg transition-colors"
>
  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth={2} className="w-4 h-4">
    <path strokeLinecap="round" strokeLinejoin="round" d="M3 9a2 2 0 012-2h.93a2 2 0 001.664-.89l.812-1.22A2 2 0 0110.07 4h3.86a2 2 0 011.664.89l.812 1.22A2 2 0 0018.07 7H19a2 2 0 012 2v9a2 2 0 01-2 2H5a2 2 0 01-2-2V9z"/>
    <circle cx="12" cy="13" r="3"/>
  </svg>
  Capturar
</button>
```

- [ ] **Step 4: Renderizar el asistente condicionalmente**

Al final del JSX del componente, antes del último `</div>` de cierre:

```jsx
{mostrarCaptura && (
  <CapturaAsistente360
    habitacion={habitacion}
    onClose={() => setMostrarCaptura(false)}
    onSuccess={() => {
      cargarImagenes();
      if (onUpdate) onUpdate();
    }}
  />
)}
```

- [ ] **Step 5: Commit**

```bash
git add frontend/src/pages/habitacion/Imagenes360Manager.jsx
git commit -m "feat: integrar CapturaAsistente360 en Imagenes360Manager"
```

---

## Task 9: Verificación final

- [ ] **Step 1: Instalar uuid en backend si no está instalado**

```bash
cd backend && node -e "import('uuid').then(m=>console.log('uuid OK')).catch(()=>console.log('instalar'))"
```

Si dice "instalar":
```bash
npm install uuid
```

- [ ] **Step 2: Reiniciar backend y abrir frontend**

```bash
# Terminal 1
cd backend && npm run dev

# Terminal 2
cd frontend && npm run dev
```

- [ ] **Step 3: Probar flujo completo en el celular**

1. Ir a Sistema → Habitaciones → seleccionar habitación → Tour 360°
2. Presionar botón "Capturar"
3. Aceptar permiso de cámara
4. Girar el celular en todas direcciones hasta ver 6+ fotos
5. Escribir título y presionar "Crear 360°"
6. Esperar ~30–60 seg (stitching Python)
7. Verificar que la nueva imagen aparece en la lista y funciona en el visor

- [ ] **Step 4: Commit final**

```bash
git add -A
git commit -m "feat: sistema completo de captura 360° desde cámara del celular"
```
